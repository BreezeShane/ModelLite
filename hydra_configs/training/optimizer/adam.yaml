# _target_: torch.optim.Adam
name: "Adam"
lr: 0.001
weight_decay: 0.0001
betas: [0.9, 0.999]
eps: 1e-8

param_groups:
  - param_names: ["features", "backbone", "encoder"]
    lr: 0.0001
    weight_decay: 0.0
  - param_names: ["classifier", "head", "decoder"]
    lr: 0.001
    weight_decay: 0.0001